# V.GOLF 스코어카드 — 포트폴리오 정리

> 레거시 스크린골프 스코어카드 웹뷰를 **인수인계받아 단독 유지보수·고도화**.
> 신규 기능 개발부터 전면 리디자인, 모바일 웹뷰 호환성 트러블슈팅, 리팩토링, 배포까지 담당.

---

## 1. 프로젝트 개요

- **서비스**: 스크린골프(V.GOLF) 라운드 결과를 **개인 / 팀 / 단체(시상 포함) 스코어카드**로 보여주고, 카카오·네이버 등 **인앱브라우저 웹뷰**에서 이미지로 **저장·공유**하는 모바일 웹.
- **참여 형태**: 원 개발자에게서 **프로젝트 인수인계 후 단독 유지보수/고도화**.
- **기간**: 2026.01 ~ 2026.08 (약 8개월) · 본인 커밋 **47건** (계정: `seung` / `seungeun` / `chloe_choi`)
- **기술 스택**
    - Frontend: **React 18 + TypeScript**, **Vite (SWC)**
    - 상태관리/라우팅: **Recoil**, **React Router v7** (파일 기반 라우팅)
    - UI: **SCSS Modules**, react-slick
    - 기능: **i18next (ko/en 다국어)**, **html2canvas (카드 이미지 저장)**, axios
    - Infra: **Gitea Actions CI/CD** (`.gitea/workflows/deploy.yml`)

---

## 2. 담당 역할

프론트엔드 유지보수·고도화 **단독 담당** — 기능 개발 · UI 리디자인 · 버그/호환성 트러블슈팅 · 리팩토링 · 배포 파이프라인 운영.

---

## 3. 핵심 성과 (하이라이트)

### ⭐ 3-1. 모바일 인앱브라우저 "이미지 저장" 문제 해결 (대표 성과)
- **문제**
    - 카카오/네이버 등 **인앱브라우저 웹뷰에서 스코어카드 이미지 저장(다운로드)이 동작하지 않음**.
    - 저장된 이미지가 **뿌옇게(저해상도)** 출력되는 품질 이슈.
- **해결**
    - `devicePixelRatio` 기반 **스케일 처리로 선명도 개선** — 뿌옇게 나오는 문제 해결.
    - 인앱브라우저 환경 대응 **저장 오버레이(`SaveImageOverlay`)** + 캡처 플로우 재설계.
    - 캡처 로직을 **`util/captureImage.ts` + 커스텀 훅 `useSaveCardImage`** 로 추출해 team/scores 페이지에서 공통 사용.
    - **원인 분석·환경별 대응·재발 방지를 담은 트러블슈팅 문서(`docs/image-save-troubleshooting.md`, 331줄) 작성.**
- **어필 포인트**: 사용자 실기기·웹뷰 파편화 문제를 **재현 → 분석 → 해결 → 문서화**까지 마무리한 실무형 문제 해결 능력. 재사용 가능한 훅/유틸/컴포넌트로 구조화.

### 3-2. i18n 리소스 번들 인라인화 (브라우저 호환성)
- 다국어 리소스(json)를 `public/`에서 HTTP 로드하던 방식 → **`src/`로 옮겨 번들에 인라인화**.
- HttpApi 로딩 시 발생하던 **네트워크/캐시/MIME/타이밍 이슈 제거**로 초기 렌더 안정화.

### 3-3. 스코어카드 전면 리디자인 + 컴포넌트 공통화 (리팩토링)
- 개인/팀 상세 · 단체 결과(scores) 페이지 **신규 디자인 적용** (히어로 헤더 · 순위 메달 · 시상 카드).
- team/scores의 히어로 헤더를 **공통 컴포넌트 `ScoreHero`로 추출**해 중복 제거(팀 전용 스타일 약 −122줄).
- 인라인 SVG **아이콘 모듈화**, 반복 JSX **배열 기반 렌더링**, 포맷 로직 **util 분리**.
- 무중단 전환을 위한 **백업 페이지 전략**(`/team_bu`, `/scores_bu`)으로 리스크 최소화.

### 3-4. 스코어 연산 로직 정비
- 파 기준을 **`holePar` 기반**으로, 스코어를 **실타수 기반 연산**으로 변경 + **검증 로직 추가**.
- 파 대비 `+` 부호를 합계에만 표기하는 등 **표기 규칙 일원화**.

### 3-5. 설문(Survey) 기능 개발
- 설문 페이지 신규 개발 · **API 연동** · 별점 인터랙션.
- **POST 응답 상태 처리**(완료/중복/오류), **204 No Content 대응**, 반응형 처리.

### 3-6. 배포/운영 (CI/CD)
- **Gitea Actions `deploy.yml`** 관리(성공/실패 알림), 운영 포트·API 연결 정보 운영.

---

## 4. 이력(타임라인 요약)

| 시기 | 작업 |
|---|---|
| 2026.01–02 | 설문 기능 개발(API 연동·상태 처리·반응형), 로딩/204 처리 |
| 2026.02–03 | 배포 파이프라인(deploy.yml)·포트/API![스크린샷 2026-09-04 오후 1.33.25.png](../../Desktop/%EC%8A%A4%ED%81%AC%EB%A6%B0%EC%83%B7%202026-09-04%20%EC%98%A4%ED%9B%84%201.33.25.png) 운영 정보 관리 |
| 2026.06 | 단체 결과(scores) 페이지 추가·UI, **i18n 번들 인라인화** |
| 2026.07–08 | **스코어카드 전면 리디자인**, 히어로 공통화, 아이콘/유틸 리팩토링 |
| 2026.08 | **인앱브라우저 이미지 저장·선명도 문제 해결 + 문서화**, 실타수 연산, 의존성 안정화 |

---

## 5. 포트폴리오 어필 포인트 (요약)

- **레거시 인수인계 후 단독 유지보수·고도화** — 기존 코드 파악 + 안전한 개선.
- **모바일 웹뷰(인앱브라우저) 파편화 실전 트러블슈팅 + 문서화** — 눈에 띄는 차별점.
- **리팩토링/공통화**(공통 컴포넌트·훅·유틸)로 **유지보수성** 개선.
- **국제화(i18n)·CI/CD** 등 프론트 전 범위 커버.

---

*정리 기준: git 이력에서 본인 계정(`plumxeun@gmail.com` → seung/chloe_choi, `seungeun@pluxity.com` → seungeun) 커밋 47건 분석.*